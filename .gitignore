node_modules/
.env
*.log
dist/
out/
# 测试仅本地（开源项目不外泄测试项；本机开发、无 CI，发版前由 prepublishOnly 自动跑一次，主理人 2026-07-26 定）
# —— 这是决策不是缺陷：不要再把它当作「忘了入库」提改进
#
# 2026-09-01 起 test/ 是本仓的 submodule（见 .gitmodules），指向 Gitea 私仓 Gitruck/cli-test，
# 与 openspec/ 同一思路。本条对那个 gitlink 条目**无效**（它是 `submodule add -f` 加的），
# 保留本条是兜底：万一哪天 submodule 被 deinit、test/ 变回普通目录时仍有屏蔽。
# 真正的硬闸是 .git/hooks/pre-commit（现已同时拦 openspec/ 与 test/ 的**带斜杠**路径）。
#
# 迁出的直接动因：此前是「半跟踪」态——本条屏蔽着，却靠 `git add -f` 塞进去 71 个文件，
# 而盘上实有 153 个。公开仓里的测试集长期只有一半，且新增测试会被 `git add .` 静默漏掉。
#
# 2026-09-01 已用 `git filter-repo --path-glob 'test/*' --invert-paths` **清洗历史**并强推两个远端：
# 历史中 87 个 test/ 路径（跨 65 个提交、2026-07-02 起）全部摘除，含 8 个带 `X:/Hocassian/...`
# 真机盘绝对路径的文件。清洗后 main 由 335 → 331 个提交（丢掉的 4 条经逐条核对**全是纯 test/ 提交**，
# 清空后成空提交被自动丢弃），**工作树 tree hash 逐字节不变**。
# 备份在 `D:/file/tmp/cli-history-backup-20260901-190803/`（本地 / Gitea / GitHub 三份镜像）。
test/
# 本机工件仅本地（真机验收清单 .plans/*-checklist.md 等留本机、不入 git，主理人 2026-07-26 定）
# 2026-08-11 主理人拍板：本目录已迁至 openspec/.plans/，与 openspec 工件同进一个 Gitea 私仓统一协作。
# 本条保留作兜底——防止今后有人在仓根重建 .plans/ 又漏进公开仓。
.plans/
.DS_Store

# IDE / 编辑器
.idea/
.vscode/
*.tgz
.test-build/
# npm lock not used - bun.lock is canonical
package-lock.json

# OpenSpec 工件仅本地（开源仓不外泄开发信息，主理人 2026-07-10 定）
# openspec/ —— 留本机、不入 git（2026-08-06 主理人拍板反转 P10）：
# 本仓是公开 GitHub 仓，0c153b8 曾专门做过「开源仓脱敏」把工件移出 git；
# P10（08-05）纳入时遗忘了该前提，08-06 泄漏后历史已 filter-repo 重写清除。
# ⚠️ 永不移除本条：openspec 工件含内部主机/供应商/架构信息，归档历史备份走本机方案。
#
# 2026-08-11 起 openspec/ 是本仓的 submodule（见 .gitmodules），指向 Gitea 私仓
# Gitruck/cli-openspec。本条对那个 gitlink 条目**无效**（它是 `submodule add -f` 加的），
# 但工件内容照旧零外泄——submodule 机制下父仓只存一个 commit hash，不存任何内容。
# 保留本条是兜底：万一哪天 submodule 被 deinit，openspec/ 变回普通目录时仍有屏蔽。
# 真正的硬闸是 .git/hooks/pre-commit（装法见 openspec/hooks/install.sh）。
openspec/

# 真机跑 CLI 时落在仓根的产物目录（2026-08-20 补）。
# 这些是 `gtrk tool` / `gtrk project init` 等命令的默认输出落点，含音频、视频工程
# 与**素材绝对路径**。发版前审计实测仓根积了 7 个，一次 `git add -A` 就会把它们
# 连同素材路径提进这个**公开 GitHub 仓**。全部加前导 / 只匹配仓根，不影响 test/fixtures。
/audio_tts_clone-*/
/tts-*-video-project-*/
/qc-verify-*/
/anchor-verify-*/

# 2026-08-23 补：上面「按名字逐个列举」的写法挡不住新起的走查工作区——
# yellowstone-final-260821/（279MB 成片 + 含素材绝对路径的 broll-plan.json）
# 和 yellowstone-linesplit-260821/ 就是这样漏进 git status 的。改挂两条通用闸：
#
# 1) 走查工作区一律落到仓根 .runs/ 下（跑命令时 `--out .runs/<名字>`），整目录屏蔽，
#    今后再起什么名字都不用回来改这个文件。
/.runs/
# 2) 单条命令的 JSON 回执落在仓根的（audio-lay-out.json / render-out.json 这类，
#    正文里带 E:\file\... 绝对路径），按后缀兜底。日志已由上面的 *.log 覆盖。
/*-out.json

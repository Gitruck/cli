# 复制为 .env 后填写。bun 会自动加载 .env（无需 dotenv）。

# 云端 API 根地址（不带末尾斜杠）
GITRUCK_API_BASE=https://api.ai-mcn.tv:10000

# API Key（鉴权 Header Authorization 的裸值，非 Bearer）
GITRUCK_API_KEY=gc_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
